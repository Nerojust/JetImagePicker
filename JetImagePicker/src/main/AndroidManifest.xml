<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Declared explicitly so the Play Store does not implicitly derive these as *required*
         features from the permissions above, which would filter out devices lacking a camera or
         microphone from installing any app that depends on this library - including apps that
         only use the image picker. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
</manifest>
